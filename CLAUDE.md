# CLAUDE.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues on the `Tincan0325/SMoEA` fork, driven via the `gh` CLI (pass `--repo Tincan0325/SMoEA` explicitly — this clone also has an `upstream` remote). See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles use their default label strings: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and one `docs/adr/` at the repo root (created lazily, absent today). See `docs/agents/domain.md`.
